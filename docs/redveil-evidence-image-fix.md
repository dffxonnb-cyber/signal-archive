# Redveil evidence image fix

The public portfolio keeps the existing evidence card paths but redirects the five Redveil SVG wrapper URLs to their source PNG captures. This removes the nested external-image dependency that produced broken image placeholders in browsers while preserving the existing captions, dimensions, and project-detail ordering.
