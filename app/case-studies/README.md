# Case Studies layout

The route-level `layout.tsx` and `readability.module.css` keep the Decision File cards readable without changing the shared portfolio card system. The scope aligns the rail and content toplines, reduces nested panel density, widens text rhythm, and preserves responsive stacking.
